# --- deps ---
/node_modules

# --- Next build output ---
/.next
/.next/cache
/out

# --- Environment files (no subir secretos) ---
.env
.env.local
.env.*.local
.env.*        # <-- ignora .env.production, .env.staging, etc.
!.env.example
!env.example
!.env.sample
!env.sample
# Asegura que los ejemplos SÍ se suben
!.env.example
!env.example
!.env.sample
!env.sample

# --- Prisma ---
# Mantén migrations en el repo (NO ignores prisma/migrations)
# Ignora la vieja DB local de SQLite si aún existe
prisma/dev.db

# --- Vercel ---
.vercel

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Editor / OS ---
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/settings.json

# --- Miscelánea útil ---
.eslintcache
coverage
*.tsbuildinfo

